/**
 * Section IDs for navigation and scrolling
 */
export const SECTION_IDS = {
  home: "home",
  services: "services",
  about: "about",
  projects: "projects",
  contact: "contact"
} as const;

/**
 * Image URLs placeholder object for future usage
 * This will be populated with actual image URLs as needed
 */
export const IMAGE_URLS = {
  // Placeholder for future image URLs
} as const;

/**
 * Site brand name
 */
export const SITE_BRAND = "DMK" as const;

